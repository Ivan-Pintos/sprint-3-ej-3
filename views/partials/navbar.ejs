<nav class="navbar navbar-dark bg-dark fixed-top p-0">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Hack the blog</a>
    <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
    <a class="btn btn-primary px-2 py-0" href="/logout">Logout</a>
    <% } else { %>
    <a class="btn btn-primary px-2 py-0" href="/admin">Admin</a>
    <% } %>
  </div>
</nav>
