<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Registrarse</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/styleLogin.css" />
    <link rel="stylesheet" href="/css/navbar.css" />
  </head>
  <body>
    <div
      class="container divLogin w-100 d-flex flex-column justify-content-center align-items-center"
    >
      <div
        class="rounded bg-white w-50 shadow-lg h-100 m-5 d-flex flex-column justify-content-center align-items-center"
      >
        <h3 class="text-muted tracking-in-expand textSecond"><b>Register</b></h3>
        <hr />
        <form method="POST" class="w-75 gap-2 d-flex flex-column" action="/register">
          <div class="d-flex">
            <div class="input-container">
              <input
                type="text"
                id="firstname"
                name="firstname"
                class="input-field"
                placeholder="Ingresar Nombre..."
                autocomplete="off"
                required
              />
              <label for="input-field" class="input-label">Primer nombre.</label>
              <span class="input-highlight"></span>
            </div>
            <div class="input-container">
              <input
                type="text"
                id="lastname"
                name="lastname"
                class="input-field"
                placeholder="Ingresar Apellido..."
                autocomplete="off"
                required
              />
              <label for="input-field" class="input-label">Apellido.</label>
              <span class="input-highlight"></span>
            </div>
          </div>
          <div class="input-container">
            <input
              type="email"
              id="email"
              name="email"
              class="input-field"
              placeholder="Ingresar Email..."
              autocomplete="off"
              required
            />
            <label for="input-field" class="input-label">Email.</label>
            <span class="input-highlight"></span>
          </div>
          <div class="input-container">
            <input
              type="password"
              id="password"
              name="password"
              class="input-field input-class"
              placeholder="Ingresar Contraseña..."
              autocomplete="off"
              required
            />
            <label for="input-field" class="input-label">Password.</label>
            <span class="input-highlight"></span>
          </div>
          <label for="role" class="form-label">Selecciona un rol</label>
          <select class="form-select" id="role" name="role">
            <option selected value="Reader">Lector</option>
            <option value="Writer">Escritor</option>
          </select>
          <% if (flash.error && flash.error.length > 0) { %>
          <div class="alert alert-danger">
            <% flash.error.forEach(message => { %>
            <p class="error-text"><%= message %></p>
            <% }); %>
          </div>
          <% } %>
          <div class="d-flex flex-row justify-content-center gap-3 align-items-center">
            <button class="effectButton btn btn-success text-center w-25" type="submit">
              <span></span>
              <span></span>
              <span></span>
              <span></span> Sign Up
            </button>
            <a class="effectButton2 btn btn-outline-success text-center w-25" href="/login"
              >Sign In</a
            >
          </div>
        </form>
      </div>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
