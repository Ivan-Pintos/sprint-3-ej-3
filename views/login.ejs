<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Iniciar sesion</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/styleLogin.css" />
  </head>
  <body>
    <div
      class="container divLogin w-100 d-flex flex-column justify-content-center align-items-center"
    >
      <div
        class="rounded w-50 shadow h-75 d-flex flex-column justify-content-around align-items-center"
      >
        <h1 class="text-muted mt-5">Login</h1>
        <form method="POST" class="w-75 gap-2 m-2 d-flex flex-column" action="/login">
          <input type="hidden" id="url" name="url" value="<%= redirectTo %>" />
          <div class="input-container">
            <input
              type="email"
              id="email"
              name="email"
              class="input-field"
              placeholder="Ingresar Email..."
              autocomplete="off"
              required
            />
            <label for="input-field" class="input-label">Email.</label>
            <span class="input-highlight"></span>
          </div>
          <div class="input-container">
            <input
              type="password"
              id="password"
              name="password"
              class="input-field"
              placeholder="Ingresar Contraseña..."
              autocomplete="off"
              required
            />
            <label for="input-field" class="input-label">Password.</label>
            <span class="input-highlight"></span>
          </div>
          <% if (message && message.length > 0) { %>
          <div class="alert alert-danger"><%= message %></div>
          <% } %>
          <div class="d-flex flex-row justify-content-center gap-3 align-items-center">
            <button class="btn btn-success text-center" type="submit">Sign In</button>
            <a class="btn btn-outline-success text-center" href="/register">Sign Up</a>
          </div>
        </form>
      </div>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
